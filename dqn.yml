apiVersion: batch/v1
kind: Job
metadata:
  name: dqn-manager-deployment
  labels:
    app: dqn-manager
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dqn-manager
  template:
    metadata:
      labels:
        app: dqn-manager
    spec:
      containers:
      - name: manager
        image: pkoperek/dqn-manager:latest
        imagePullPolicy: Always
      restartPolicy: Never

---
kind: Service
apiVersion: v1
metadata:
  name: gateway-service
spec:
  type: NodePort
  selector:
    app: gateway
  ports:
  - protocol: TCP
    port: 25333
    targetPort: 25333

---
kind: Deployment
apiVersion: apps/v1
metadata:
  name: gateway-deployment
  labels:
    app: gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gateway
  template:
    metadata:
      labels:
        app: gateway
    spec:
      containers:
      - name: cloudsimplus-gateway
        image: pkoperek/cloudsimplus-gateway:latest
        imagePullPolicy: Always
